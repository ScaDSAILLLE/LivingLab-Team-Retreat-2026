# Starter Code Area

This folder is reserved for later starter-code material.

## Current Status
- No executable starter application is present yet.
- The retreat currently documents a planned Nanobot-based setup using `uv`, Nanobot WebUI, opencode, and a Raspberry Pi target system.
- Do not invent runnable commands here until the corresponding files exist.

## Planned Topics
- `pyproject.toml` for a minimal `uv` project, if custom glue code is needed.
- `.env.example` with placeholders only.
- Nanobot provider configuration notes for `llm.scads.ai` or another approved endpoint.
- Optional API adapter for `nanobot serve` if the WebUI alone is not sufficient.
- RPi setup/start scripts after the target architecture is confirmed.
